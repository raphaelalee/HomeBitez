<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register - HomeBitez</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Sniglet:wght@700;900&display=swap" rel="stylesheet">

    <style>
        /* Reset default margins and paddings */
        html, body {
            margin: 0;
            padding: 0;
            height: 100%;
            background-color: #f8f3ed;
            font-family: Arial, sans-serif;
        }

        .container-box {
            display: flex;
            min-height: 100vh; /* ensure full viewport height */
        }

        /* LEFT SIDE */
        .left-box {
            width: 50%;
            background: white;
            padding: 80px; /* extra top padding to prevent logo cutoff */
            display: flex;
            flex-direction: column;
            justify-content: flex-start; /* align from top */
            flex-grow: 1;
            box-sizing: border-box; /* includes padding in height */
        }

        .logo {
            font-family: 'Sniglet', cursive;
            font-size: 40px;
            font-weight: 900;
            margin-bottom: 30px;
            line-height: 1;
            text-shadow: 2px 2px 2px rgba(0,0,0,0.1);
        }
        .logo span.home { color: #f4b400; }
        .logo span.bitez { color: #FF8C1A; }

        h4 { margin-bottom: 10px; }
        p { margin-bottom: 20px; }

        /* RIGHT SIDE */
        .right-box {
            width: 50%;
            background-color: #ffe9d6; /* match body for seamless look */
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 40px;
            overflow: hidden;
        }

        .right-box img {
            width: 90%;
            max-width: 500px;
            height: 100%;
            max-height: 800px;
            border-radius: 10%; /* optional rounded corner */
            object-fit: cover;
            box-shadow: 0 8px 20px rgba(0,0,0,0.15);
        }

        /* FORM STYLING */
        .form-control {
            background: #f4f4f4;
            border-radius: 25px;
            height: 45px;
            border: none;
        }

        .register-btn {
            width: 100%;
            height: 45px;
            background-color: black;
            color: white;
            border-radius: 25px;
            border: none;
            font-size: 16px;
            margin-top: 10px;
        }

        .social-btn {
            display: flex;
            align-items: center;
            gap: 10px;
            width: 100%;
            height: 45px;
            background: white;
            border-radius: 25px;
            border: 1px solid #ddd;
            padding: 5px 15px;
        }

        .social-btn img { height: 20px; }

        a { text-decoration: none; color: #666; }
    </style>
</head>

<body>
<div class="container-box">

    <!-- LEFT SIDE -->
    <div class="left-box">
        <div class="logo">
            <span class="home">Home</span><span class="bitez">Bitez</span>
        </div>

        <h4>Create Your Account!</h4>
        <p>Fill in the form to get started</p>

        <% if (error && error.length > 0) { %>
            <div class="alert alert-danger"><%= error %></div>
        <% } %>

        <form action="/signup" method="POST">
            <input type="text" name="username" placeholder="Username" class="form-control mb-3" required>
            <input type="email" name="email" placeholder="Email" class="form-control mb-3" required>
            <input type="text" name="contact" placeholder="Contact" class="form-control mb-3" required>
            <input type="password" name="password" placeholder="Password" class="form-control mb-3" required>
            <input type="password" name="confirmPassword" placeholder="Confirm Password" class="form-control mb-3" required>
            
            <button class="register-btn" type="submit">Register</button>
        </form>

        <div class="text-center my-3">or sign up with</div>

        <button class="social-btn mb-2">
            <img src="https://cdn-icons-png.flaticon.com/512/2991/2991148.png"> Sign up with Google
        </button>

        <button class="social-btn mb-4">
            <img src="https://cdn-icons-png.flaticon.com/512/733/733547.png"> Sign up with Facebook
        </button>

        <div class="text-center">
            Already have an account? <a href="/login">Login</a>
        </div>
    </div>

    <!-- RIGHT SIDE IMAGE -->
    <div class="right-box">
        <img src="/images/flatlay-iron-skillet-with-meat-and-other-food.jpg" alt="Food">
    </div>

</div>
</body>
</html>

