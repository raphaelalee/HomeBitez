<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= product.name %></title>
  <link rel="stylesheet" href="/css/product.css">

<style>
body {
  font-family: Arial, sans-serif;
  background: #fff4e9;
  margin: 0;
}

.navbar {
  display: flex;
  justify-content: space-between;
  padding: 20px 40px;
}

.product-page {
  display: flex;
  padding: 40px;
  gap: 60px;
}

.left {
  width: 50%;
}

.product-image {
  width: 100%;
  border-radius: 10px;
}

.right {
  width: 50%;
}

.price, .points {
  font-weight: bold;
}

.quantity {
  display: flex;
  align-items: center;
  gap: 10px;
  margin: 20px 0;
}

.customization input {
  width: 100%;
  padding: 10px;
}

.actions {
  display: flex;
  gap: 20px;
  margin-top: 30px;
}

.btn-primary {
  background: #f4b400;
  border: none;
  padding: 12px 20px;
  border-radius: 8px;
  cursor: pointer;
}

.btn-secondary {
  border: 1px solid #ccc;
  padding: 12px 20px;
  border-radius: 8px;
  text-decoration: none;
  color: black;
}
</style>

</head>
<body>

<header class="navbar">
  <h1 class="logo">HomeBites</h1>
  <nav>
    <a href="/">Home</a>
    <a href="/menu">Menu</a>
    <a href="#">Contact us</a>
  </nav>
</header>

<main class="product-page">

  <div class="left">
    <h2>The Sandwich Corner</h2>
    <img src="/Images/<%= product.image %>" alt="<%= product.name %>" class="product-image">
  </div>

  <div class="right">
    <h1><%= product.name %></h1>

    <p class="description">
      <%= product.description %>
    </p>

    <p class="price">Price: $<%= product.price %></p>
    <p class="points">Points: <%= Math.floor(product.price) %> points</p>

    <div class="quantity">
      <button>-</button>
      <span>1</span>
      <button>+</button>
    </div>

    <div class="customization">
      <label>Customization (Optional)</label>
      <input type="text" placeholder="Type here...">
    </div>

    <div class="actions">
      <a href="/menu" class="btn-secondary">Back to Menu</a>

      <form action="/add-to-cart" method="POST">
        <input type="hidden" name="product_id" value="<%= product.id %>">
        <input type="hidden" name="name" value="<%= product.name %>">
        <input type="hidden" name="price" value="<%= product.price %>">
        <button type="submit" class="btn-primary">Add to basket</button>
      </form>
    </div>

  </div>

</main>

</body>
</html>