<!--
I declare that this code was written by me.
I will not copy or allow others to copy my code.
I understand that copying code is considered as plagiarism.

Student Name: Marla
Student ID: 24014514
Class: E63C c004
Date created: February 5, 2026
-->

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Singpass 2FA - HomeBitez</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Sniglet:wght@700;900&display=swap" rel="stylesheet">
    <style>
        body {
            background-color: #f8f3ed;
            font-family: Arial, sans-serif;
        }
        .card {
            border-radius: 16px;
            box-shadow: 0 8px 24px rgba(0,0,0,0.08);
        }
        .logo {
            font-family: 'Sniglet', cursive;
            font-size: 32px;
            font-weight: 900;
            line-height: 1;
            text-shadow: 2px 2px 2px rgba(0,0,0,0.08);
        }
        .logo span.home { color: #f4b400; }
        .logo span.bitez { color: #FF8C1A; }
        .form-control {
            background: #f4f4f4;
            border-radius: 25px;
            height: 45px;
            border: none;
            text-align: center;
            letter-spacing: 3px;
            font-size: 18px;
        }
        .primary-btn {
            width: 100%;
            height: 45px;
            background-color: black;
            color: white;
            border-radius: 25px;
            border: none;
            font-size: 16px;
        }
        .secondary-btn {
            width: 100%;
            height: 45px;
            background-color: white;
            color: #111;
            border-radius: 25px;
            border: 1px solid #ddd;
            font-size: 16px;
        }
        .hint {
            font-size: 12px;
            color: #666;
        }
    </style>
</head>
<body>
<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-6 col-lg-5">
            <div class="card p-4">
                <div class="mb-3">
                    <div class="logo">
                        <span class="home">Home</span><span class="bitez">Bitez</span>
                    </div>
                </div>
                <h4 class="mb-1">Singpass 2FA</h4>
                <p class="text-muted mb-3">A verification code was generated and printed in the server terminal.</p>

                <% if (error && error.length > 0) { %>
                    <div class="alert alert-danger"><%= error %></div>
                <% } %>
                <% if (success && success.length > 0) { %>
                    <div class="alert alert-success"><%= success %></div>
                <% } %>

                <form action="/singpass/2fa/verify" method="POST" class="mb-3">
                    <input type="text" name="code" placeholder="Enter 6-digit code" class="form-control mb-3" required>
                    <button class="primary-btn" type="submit">Verify</button>
                </form>

                <form action="/singpass/2fa/resend" method="POST">
                    <button class="secondary-btn" type="submit">Resend Code (Terminal)</button>
                </form>

                <div class="hint mt-3">Mock flow only. No SMS/email is sent.</div>
            </div>
        </div>
    </div>
</div>
</body>
</html>

