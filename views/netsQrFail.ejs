<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>NETS Payment Failed</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body { background: #faefe5; font-family: Arial, sans-serif; }
    .card { max-width: 520px; margin: 60px auto; padding: 24px; border-radius: 14px; }
  </style>
</head>
<body>
  <div class="card bg-white shadow-sm">
    <h4 class="mb-2">NETS Payment Failed</h4>
    <p class="text-muted mb-3"><%= errorMsg || "Payment failed. Please try again." %></p>
    <a href="/checkout" class="btn btn-warning">Back to checkout</a>
  </div>
</body>
</html>
