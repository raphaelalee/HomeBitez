<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('../partials/head', { title: 'Inventory - HomeBitez' }) %>

  <style>
    body {
      margin: 0;
      padding: 0;
      font-family: "Poppins", sans-serif;
      font-weight: 400;
      color: #2a241e;
    }

    /* HERO HEADER */
    .hero {
      background: linear-gradient(135deg, #f3e3cb, #f8f1e7);
      padding: 46px 40px 68px;
      border-radius: 0 0 28px 28px;
      border-bottom: 1px solid #eadac6;
    }

    .hero-title {
      font-size: 34px;
      font-weight: 600;
      letter-spacing: -0.01em;
    }

    .hero-sub {
      margin-top: 8px;
      font-size: 15px;
      font-weight: 400;
      color: rgba(42,36,30,0.7);
    }

    .hero-buttons {
      margin-top: 20px;
      display: flex;
      gap: 15px;
      flex-wrap: wrap;
    }

    .hero-btn {
      padding: 11px 20px;
      border-radius: 999px;
      font-size: 14px;
      font-weight: 500;
      text-decoration: none;
      cursor: pointer;
    }

    .add-product-btn {
      background: #d59b2d;
      color: #fff;
      box-shadow: 0 8px 18px rgba(213,155,45,0.22);
    }

    .ghost-btn {
      background: rgba(255,255,255,0.78);
      color: #2a241e;
      border: 1px solid #eadac6;
    }

    /* STATS */
    .stats-row {
      display: flex;
      gap: 16px;
      margin-top: 30px;
      flex-wrap: wrap;
    }

    .stat-box {
      background: #fffdf9;
      border: 1px solid #eadac6;
      padding: 14px 18px;
      border-radius: 14px;
      width: 180px;
      box-shadow: 0 8px 18px rgba(42,36,30,0.07);
    }

    .stat-number {
      font-size: 22px;
      font-weight: 600;
    }

    .stat-label {
      font-size: 12px;
      font-weight: 400;
      color: rgba(42,36,30,0.65);
    }

    /* CONTENT */
    .content {
      background: #fffdf9;
      margin: -36px auto 36px;
      padding: 28px;
      width: 94%;
      border-radius: 18px;
      border: 1px solid #eadac6;
      box-shadow: 0 10px 24px rgba(42,36,30,0.08);
    }

    .section-title {
      font-size: 20px;
      font-weight: 600;
      margin-bottom: 16px;
    }

    /* SEARCH */
    .top-controls {
      display: flex;
      justify-content: space-between;
      gap: 15px;
      margin-bottom: 25px;
      flex-wrap: wrap;
    }

    .search-box {
      display: flex;
      flex: 1;
      gap: 10px;
      min-width: 260px;
    }

    .search-input {
      flex: 1;
      padding: 12px 14px;
      border-radius: 999px;
      border: 1px solid #eadac6;
      font-weight: 400;
    }

    .search-btn {
      padding: 12px 18px;
      background: #d59b2d;
      border-radius: 999px;
      color: white;
      font-weight: 600;
      border: none;
    }

    .sort-select {
      padding: 12px 14px;
      border-radius: 999px;
      border: 1px solid #eadac6;
      font-weight: 400;
    }

    /* PRODUCT GRID */
    .product-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(240px, 1fr));
      gap: 28px;
    }

    .product-card {
      background: #fff;
      padding: 18px;
      border-radius: 22px;
      border: 1px solid #eadac6;
      box-shadow: 0 10px 26px rgba(42,36,30,0.08);
      position: relative;
    }

    .product-img {
      width: 100%;
      height: 150px;
      object-fit: cover;
      border-radius: 16px;
      margin-bottom: 10px;
    }

    .product-name {
      font-size: 16px;
      font-weight: 600;
    }

    .product-price {
      font-size: 14px;
      font-weight: 600;
      margin-bottom: 6px;
    }

    .meta {
      font-size: 12px;
      font-weight: 400;
      color: rgba(42,36,30,0.65);
      margin-bottom: 10px;
      line-height: 1.4;
    }

    /* ICON ACTIONS */
    .icon-actions {
      position: absolute;
      top: 12px;
      right: 12px;
      display: flex;
      flex-direction: column;
      gap: 10px;
    }

    .icon-btn {
      width: 40px;
      height: 40px;
      border-radius: 999px;
      background: #fffaf3;
      border: 1px solid #eadac6;
      display: flex;
      align-items: center;
      justify-content: center;
      cursor: pointer;
      padding: 0;
    }

    .edit-btn svg { stroke: #1f7a4a; stroke-width: 2; }
    .delete-btn svg { stroke: #b42318; stroke-width: 2; }

    /* REPLENISH */
    .replenish-row {
      display: flex;
      gap: 10px;
    }

    .replenish-input {
      flex: 1;
      padding: 10px 12px;
      border-radius: 999px;
      border: 1px solid #eadac6;
      font-weight: 400;
    }

    .replenish-btn {
      background: #2a241e;
      border-radius: 999px;
      color: white;
      font-weight: 600;
      border: none;
      padding: 10px 14px;
    }

  </style>
</head>

<body>
  <%- include('../partials/navbar-bizowner') %>

  <!-- HERO -->
  <div class="hero">
    <div class="hero-title">Keep your catalogue looking sharp</div>
    <div class="hero-sub">Review, adjust, and launch products easily.</div>

    <div class="hero-buttons">
      <a href="/bizowner/add" class="hero-btn add-product-btn">+ Add Product</a>
      <a href="/menu" class="hero-btn ghost-btn">View Storefront</a>
    </div>

    <div class="stats-row">
      <div class="stat-box">
        <div class="stat-number"><%= products.length %></div>
        <div class="stat-label">Active Listings</div>
      </div>

      <div class="stat-box">
        <div class="stat-number"><%= products.reduce((a,b)=>a + Number(b.quantity||0),0) %></div>
        <div class="stat-label">Total Stock</div>
      </div>

      <div class="stat-box">
        <div class="stat-number">
          $<%= products.length ? (products.reduce((a,b)=>a+Number(b.price||0),0)/products.length).toFixed(2) : "0.00" %>
        </div>
        <div class="stat-label">Avg. Price</div>
      </div>
    </div>
  </div>

  <!-- CONTENT -->
  <div class="content">
    <div class="section-title">Manage Listings</div>

    <div class="top-controls">
      <div class="search-box">
        <input id="search" class="search-input" placeholder="Search products...">
        <button class="search-btn">Search</button>
      </div>

      <select class="sort-select">
        <option>Newest first</option>
        <option>Oldest first</option>
        <option>Price: Low → High</option>
        <option>Price: High → Low</option>
      </select>
    </div>

    <div class="product-grid">
      <% products.forEach(p => { %>
        <div class="product-card">
          <div class="icon-actions">
            <a href="/bizowner/edit/<%= p.id %>" class="icon-btn edit-btn">
              <svg viewBox="0 0 24 24" width="18" height="18" fill="none">
                <path d="M12 2l4 4-10 10H2v-4L12 2z"></path>
              </svg>
            </a>

            <form action="/bizowner/delete/<%= p.id %>" method="POST">
              <button class="icon-btn delete-btn"
                onclick="return confirm('Delete this product?')">
                <svg viewBox="0 0 24 24" width="18" height="18" fill="none">
                  <path d="M3 6h18" />
                  <path d="M8 6v14h8V6" />
                  <path d="M10 11v6" />
                  <path d="M14 11v6" />
                </svg>
              </button>
            </form>
          </div>

          <img src="/images/<%= p.image %>" class="product-img">

          <div class="product-name"><%= p.productName %></div>
          <div class="product-price">$<%= Number(p.price).toFixed(2) %></div>

          <div class="meta">
            ID: <%= p.id %><br>
            Stock: <%= p.quantity %>
          </div>

          <form action="/bizowner/replenish/<%= p.id %>" method="POST" class="replenish-row">
            <input type="number" class="replenish-input" name="quantity" placeholder="Add stock" required>
            <button class="replenish-btn">+</button>
          </form>
        </div>
      <% }) %>
    </div>
  </div>

</body>
</html>
