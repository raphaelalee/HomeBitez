<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Inventory - HomeBitez</title>

    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: "Poppins", sans-serif;
            background: #f5f6fa;
        }

        /* NAVBAR */
        .navbar {
            background: white;
            padding: 18px 40px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            border-bottom: 1px solid #eee;
        }

        .logo {
            font-size: 28px;
            font-weight: 700;
            color: #f4b400;
        }

        .nav-links a {
            margin: 0 12px;
            text-decoration: none;
            font-weight: 500;
            color: #333;
        }

        /* HERO HEADER */
        .hero {
            background: linear-gradient(135deg, #f4b400, #ffdb78);
            padding: 50px 50px 80px;
            border-radius: 0 0 35px 35px;
            color: #fff;
        }

        .hero-title {
            font-size: 38px;
            font-weight: 700;
        }

        .hero-sub {
            margin-top: 8px;
            font-size: 15px;
            opacity: 0.9;
        }

        .hero-buttons {
            margin-top: 20px;
            display: flex;
            gap: 15px;
        }

        .hero-btn {
            padding: 12px 24px;
            border: none;
            border-radius: 25px;
            font-size: 14px;
            font-weight: 600;
            cursor: pointer;
            transition: .2s;
        }

        .add-product-btn {
            background: #fff;
            color: #f4b400;
        }

        .add-product-btn:hover {
            background: rgba(255, 255, 255, 0.9);
            transform: scale(1.05);
        }

        /* STATS */
        .stats-row {
            display: flex;
            gap: 20px;
            margin-top: 40px;
        }

        .stat-box {
            background: rgba(255, 255, 255, 0.25);
            padding: 18px 25px;
            border-radius: 16px;
            width: 180px;
            color: white;
        }

        .stat-number {
            font-size: 26px;
            font-weight: 700;
        }

        .stat-label {
            opacity: 0.9;
            font-size: 14px;
        }

        /* CONTENT WRAPPER */
        .content {
            background: #fff;
            margin: -40px auto 40px;
            padding: 35px;
            width: 92%;
            border-radius: 22px;
            box-shadow: 0px 8px 25px rgba(0, 0, 0, 0.08);
        }

        .section-title {
            font-size: 22px;
            font-weight: 600;
            margin-bottom: 25px;
        }

        /* SEARCH / SORT */
        .top-controls {
            display: flex;
            justify-content: space-between;
            gap: 15px;
            margin-bottom: 25px;
        }

        .search-box {
            display: flex;
            flex: 1;
            gap: 10px;
        }

        .search-input {
            flex: 1;
            padding: 12px;
            border-radius: 12px;
            border: 1px solid #ddd;
            background: #fafafa;
        }

        .search-btn {
            padding: 12px 18px;
            background: #f4b400;
            border: none;
            border-radius: 12px;
            color: white;
            font-weight: 600;
            cursor: pointer;
        }

        .sort-select {
            padding: 12px;
            border-radius: 12px;
            border: 1px solid #ddd;
            font-size: 14px;
        }

        /* PRODUCT GRID */
        .product-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(240px, 1fr));
            gap: 28px;
        }

        .product-card {
            background: white;
            padding: 18px;
            border-radius: 18px;
            box-shadow: 0px 5px 15px rgba(0, 0, 0, 0.08);
            transition: 0.2s;
            position: relative;
        }

        .product-card:hover {
            transform: translateY(-4px);
            box-shadow: 0px 10px 22px rgba(0, 0, 0, 0.12);
        }

        .product-img {
            width: 100%;
            height: 150px;
            object-fit: cover;
            border-radius: 14px;
            margin-bottom: 10px;
        }

        .product-name {
            font-size: 17px;
            font-weight: 600;
        }

        .product-price {
            color: #1e88e5;
            font-weight: 600;
            margin-bottom: 6px;
        }

        .meta {
            opacity: 0.7;
            font-size: 12px;
            margin-bottom: 10px;
        }

        /* ================= OPTION A — CLEAN WHITE ICON BUTTONS ================= */
        .icon-actions {
            position: absolute;
            top: 12px;
            right: 12px;
            display: flex;
            flex-direction: column;
            gap: 10px;
        }

        .icon-btn {
            width: 40px;
            height: 40px;
            background: white;
            border-radius: 50%;
            border: 1px solid #eee;
            box-shadow: 0px 3px 10px rgba(0, 0, 0, 0.12);
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            transition: .2s ease;
        }

        .icon-btn:hover {
            transform: scale(1.12);
            background: #fffdf5;
            box-shadow: 0px 6px 15px rgba(0, 0, 0, 0.15);
        }

        .icon-btn svg {
            stroke-width: 2;
            stroke-linecap: round;
            stroke-linejoin: round;
        }

        .edit-btn svg {
            stroke: #17c47b;
        }

        .delete-btn svg {
            stroke: #ff4c4c;
        }

        /* REPLENISH */
        .replenish-row {
            display: flex;
            gap: 10px;
        }

        .replenish-input {
            flex: 1;
            padding: 10px;
            border-radius: 10px;
            border: 1px solid #ddd;
        }

        .replenish-btn {
            background: #1e88e5;
            border: none;
            padding: 10px 14px;
            border-radius: 10px;
            color: white;
            font-weight: 600;
            cursor: pointer;
        }
    </style>
</head>

<body>

    <!-- NAVBAR -->
    <div class="navbar">
        <div class="logo">HomeBitez</div>
        <div class="nav-links">
            <a href="/owner">Dashboard</a>
            <a href="/owner/inventory">Inventory</a>
        </div>
        <div class="profile">
            <img src="/images/default-profile.jpg" width="36" height="36" style="border-radius:50%;">
            Business Owner
        </div>
    </div>

    <!-- HERO HEADER -->
    <div class="hero">
        <div class="hero-title">Keep your catalogue looking sharp</div>
        <div class="hero-sub">Review, adjust, and launch products easily.</div>

        <div class="hero-buttons">
            <a href="/owner/add" class="hero-btn add-product-btn">+ Add Product</a>
            <button class="hero-btn" style="background:rgba(255,255,255,0.2); color:#fff;">View Storefront</button>
        </div>

        <div class="stats-row">
            <div class="stat-box">
                <div class="stat-number">
                    <%= products.length %>
                </div>
                <div class="stat-label">Active Listings</div>
            </div>
            <div class="stat-box">
                <div class="stat-number">
                    <%= products.reduce((a,b)=>a+b.quantity,0) %>
                </div>
                <div class="stat-label">Total Stock</div>
            </div>
            <div class="stat-box">
                <div class="stat-number">
                    $<%= (products.reduce((a,b)=>a+b.price,0) / products.length).toFixed(2) %>
                </div>
                <div class="stat-label">Avg. Price</div>
            </div>
        </div>
    </div>

    <!-- CONTENT -->
    <div class="content">

        <div class="section-title">Manage Listings</div>

        <div class="top-controls">
            <div class="search-box">
                <input id="search" class="search-input" placeholder="Search products...">
                <button class="search-btn">Search</button>
            </div>

            <select class="sort-select">
                <option>Newest first</option>
                <option>Oldest first</option>
                <option>Price: Low → High</option>
                <option>Price: High → Low</option>
            </select>
        </div>

        <!-- PRODUCT GRID -->
        <div class="product-grid">

            <% products.forEach(p=> { %>

                <div class="product-card" data-name="<%= p.productName.toLowerCase() %>">

                    <!-- ACTION BUTTONS -->
                    <div class="icon-actions">

                        
                        <!-- EDIT BUTTON (links to edit page) -->
                        <a href="/owner/edit/<%= p.id %>" class="icon-btn edit-btn">
                            <svg width="18" height="18" fill="none">
                                <path d="M12 2l4 4-10 10H2v-4L12 2z"></path>
                            </svg>
                        </a>


                        <!-- DELETE BUTTON -->
                        <form action="/owner/delete/<%= p.id %>" method="POST">
                            <button class="icon-btn delete-btn" onclick="return confirm('Delete this product?')">
                                <svg width="18" height="18" fill="none">
                                    <polyline points="3 6 5 6 21 6"></polyline>
                                    <path d="M19 6l-1 14H6L5 6"></path>
                                    <path d="M10 11v6"></path>
                                    <path d="M14 11v6"></path>
                                </svg>
                            </button>
                        </form>
                    </div>

                    <img src="/images/<%= p.image %>" class="product-img">

                    <div class="product-name">
                        <%= p.productName %>
                    </div>
                    <div class="product-price">$<%= p.price.toFixed(2) %>
                    </div>

                    <div class="meta">
                        ID: <%= p.id %> <br>
                            Stock: <%= p.quantity %> <br>
                                Image: <%= p.image %>
                    </div>

                    <form action="/owner/replenish/<%= p.id %>" method="POST" class="replenish-row">
                        <input type="number" min="1" class="replenish-input" name="quantity" placeholder="Add stock">
                        <button class="replenish-btn">+</button>
                    </form>

                </div>

                <% }) %>

        </div>

    </div>

</body>

</html>