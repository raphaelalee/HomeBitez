<!--
I declare that this code was written by me.
I will not copy or allow others to copy my code.
I understand that copying code is considered as plagiarism.

Student Name: Raphaela Lee
Student ID: 24009059
Class: c004
Date created: February 5, 2026
-->

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Edit Product</title>

    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: "Poppins", Arial, sans-serif;
            background: #faf7f2;
        }

        .navbar {
            background: white;
            padding: 18px 40px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            border-bottom: 1px solid #eee;
        }

        .logo {
            font-size: 26px;
            font-weight: 700;
            color: #f4b400;
        }

        .page-title {
            margin: 30px 40px 10px;
            font-size: 24px;
            font-weight: 600;
            color: #444;
        }

        .container {
            background: white;
            padding: 30px;
            width: 500px;
            margin: 20px auto;
            border-radius: 16px;
            box-shadow: 0px 4px 12px rgba(0, 0, 0, 0.08);
        }

        label {
            font-weight: 600;
            margin-top: 10px;
            display: block;
            font-size: 14px;
        }

        input, select, textarea {
            width: 100%;
            padding: 12px;
            margin-top: 5px;
            border-radius: 10px;
            border: 1px solid #ccc;
            font-size: 14px;
            background: #fff;
        }

        textarea {
            resize: vertical;
            min-height: 96px;
            line-height: 1.4;
        }

        .check-line {
            display: flex;
            align-items: center;
            gap: 10px;
            margin-top: 8px;
            color: #555;
            font-size: 14px;
        }

        .check-line input[type="checkbox"] {
            width: auto;
            margin: 0;
            transform: scale(1.05);
        }

        .tag-grid {
            margin-top: 8px;
            display: grid;
            grid-template-columns: repeat(2, minmax(0, 1fr));
            gap: 8px;
        }

        .tag-item {
            display: flex;
            align-items: center;
            gap: 8px;
            padding: 8px 10px;
            border: 1px solid #e8e0d4;
            border-radius: 10px;
            background: #fff;
            font-size: 13px;
            color: #4f4f4f;
        }

        .tag-item input[type="checkbox"] {
            width: auto;
            margin: 0;
        }

        .hint {
            margin-top: 6px;
            font-size: 12px;
            color: #777;
        }

        .image-preview {
            margin-top: 15px;
            width: 100%;
            height: 200px;
            object-fit: cover;
            border-radius: 12px;
            border: 1px solid #ddd;
        }

        button {
            padding: 12px;
            width: 100%;
            background: #f4b400;
            color: white;
            border: none;
            border-radius: 10px;
            font-size: 15px;
            font-weight: 600;
            cursor: pointer;
            margin-top: 15px;
        }

        button:hover {
            background: #d99a00;
        }

        .back {
            text-decoration: none;
            display: block;
            margin-bottom: 20px;
            color: #444;
            font-size: 14px;
        }
    </style>
</head>

<body>

    <div class="navbar">
        <div class="logo">HomeBitez</div>
    </div>

    <div class="page-title">Edit Product</div>

    <div class="container">

        <a class="back" href="/bizowner/inventory">← Back to Inventory</a>

        <% 
            const dietaryKnown = ['spicy', 'vegetarian', 'vegan', 'contains beef', 'contains chicken', 'contains seafood'];
            const allergenKnown = ['gluten', 'dairy', 'egg', 'nuts', 'shellfish', 'soy'];
            const dietarySelected = String(product.dietaryTags || '')
                .split(',')
                .map(t => t.trim().toLowerCase())
                .filter(Boolean);
            const allergenSelected = String(product.allergenTags || '')
                .split(',')
                .map(t => t.trim().toLowerCase())
                .filter(Boolean);
            const dietaryCustomExisting = String(product.dietaryTags || '')
                .split(',')
                .map(t => t.trim())
                .filter(Boolean)
                .filter(t => !dietaryKnown.includes(t.toLowerCase()))
                .join(', ');
            const allergenCustomExisting = String(product.allergenTags || '')
                .split(',')
                .map(t => t.trim())
                .filter(Boolean)
                .filter(t => !allergenKnown.includes(t.toLowerCase()))
                .join(', ');
        %>

        <form action="/bizowner/edit/<%= product.id %>" method="POST" enctype="multipart/form-data">

            <label>Product Name</label>
            <input type="text" name="productName" value="<%= product.productName %>" required>

            <label>Description</label>
            <textarea name="description" required><%= product.description || '' %></textarea>

            <label>Category</label>
            <select name="category" required>
                <option value="">Select category</option>
                <option value="International" <%= product.category === 'International' ? 'selected' : '' %>>International</option>
                <option value="Local" <%= product.category === 'Local' ? 'selected' : '' %>>Local</option>
                <option value="Dessert" <%= product.category === 'Dessert' ? 'selected' : '' %>>Dessert</option>
                <option value="Sides" <%= product.category === 'Sides' ? 'selected' : '' %>>Sides</option>
                <option value="Beverage" <%= product.category === 'Beverage' ? 'selected' : '' %>>Beverage</option>
            </select>

            <label>Price (S$)</label>
            <input type="number" step="0.01" name="price" value="<%= product.price %>" required>

            <label>Best Seller</label>
            <label class="check-line">
                <input type="checkbox" name="isBestSeller" value="1" <%= Number(product.isBestSeller || 0) ? 'checked' : '' %>>
                Mark this product as a Best Seller
            </label>

            <label>Discount (%)</label>
            <input type="number" min="0" max="90" step="1" name="discountPercent" value="<%= Number(product.discountPercent || 0) %>">
            <div class="hint">Set 0 if no discount.</div>

            <label>Dietary / Food Tags</label>
            <div class="tag-grid">
                <label class="tag-item"><input type="checkbox" name="dietaryTags" value="Spicy" <%= dietarySelected.includes('spicy') ? 'checked' : '' %>> Spicy</label>
                <label class="tag-item"><input type="checkbox" name="dietaryTags" value="Vegetarian" <%= dietarySelected.includes('vegetarian') ? 'checked' : '' %>> Vegetarian</label>
                <label class="tag-item"><input type="checkbox" name="dietaryTags" value="Vegan" <%= dietarySelected.includes('vegan') ? 'checked' : '' %>> Vegan</label>
                <label class="tag-item"><input type="checkbox" name="dietaryTags" value="Contains Beef" <%= dietarySelected.includes('contains beef') ? 'checked' : '' %>> Contains Beef</label>
                <label class="tag-item"><input type="checkbox" name="dietaryTags" value="Contains Chicken" <%= dietarySelected.includes('contains chicken') ? 'checked' : '' %>> Contains Chicken</label>
                <label class="tag-item"><input type="checkbox" name="dietaryTags" value="Contains Seafood" <%= dietarySelected.includes('contains seafood') ? 'checked' : '' %>> Contains Seafood</label>
            </div>
            <input type="text" name="dietaryTagsCustom" placeholder="Custom dietary tags (comma separated)" value="<%= dietaryCustomExisting %>">

            <label>Allergen Tags</label>
            <div class="tag-grid">
                <label class="tag-item"><input type="checkbox" name="allergenTags" value="Gluten" <%= allergenSelected.includes('gluten') ? 'checked' : '' %>> Gluten</label>
                <label class="tag-item"><input type="checkbox" name="allergenTags" value="Dairy" <%= allergenSelected.includes('dairy') ? 'checked' : '' %>> Dairy</label>
                <label class="tag-item"><input type="checkbox" name="allergenTags" value="Egg" <%= allergenSelected.includes('egg') ? 'checked' : '' %>> Egg</label>
                <label class="tag-item"><input type="checkbox" name="allergenTags" value="Nuts" <%= allergenSelected.includes('nuts') ? 'checked' : '' %>> Nuts</label>
                <label class="tag-item"><input type="checkbox" name="allergenTags" value="Shellfish" <%= allergenSelected.includes('shellfish') ? 'checked' : '' %>> Shellfish</label>
                <label class="tag-item"><input type="checkbox" name="allergenTags" value="Soy" <%= allergenSelected.includes('soy') ? 'checked' : '' %>> Soy</label>
            </div>
            <input type="text" name="allergenTagsCustom" placeholder="Custom allergen tags (comma separated)" value="<%= allergenCustomExisting %>">

            <label>Current Image</label>
            <img src="/images/<%= product.image %>" class="image-preview">

            <label>Upload New Image</label>
            <input type="file" name="imageFile" accept="image/*">

            <!-- Hidden field to keep old image if user does not upload a new one -->
            <input type="hidden" name="currentImage" value="<%= product.image %>">

            <button type="submit">Save Changes</button>
        </form>

    </div>

</body>

</html>
