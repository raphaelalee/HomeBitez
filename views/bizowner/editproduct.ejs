<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Edit Product</title>

    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: "Poppins", Arial, sans-serif;
            background: #faf7f2;
        }

        .navbar {
            background: white;
            padding: 18px 40px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            border-bottom: 1px solid #eee;
        }

        .logo {
            font-size: 26px;
            font-weight: 700;
            color: #f4b400;
        }

        .page-title {
            margin: 30px 40px 10px;
            font-size: 24px;
            font-weight: 600;
            color: #444;
        }

        .container {
            background: white;
            padding: 30px;
            width: 500px;
            margin: 20px auto;
            border-radius: 16px;
            box-shadow: 0px 4px 12px rgba(0, 0, 0, 0.08);
        }

        label {
            font-weight: 600;
            margin-top: 10px;
            display: block;
            font-size: 14px;
        }

        input {
            width: 100%;
            padding: 12px;
            margin-top: 5px;
            border-radius: 10px;
            border: 1px solid #ccc;
            font-size: 14px;
        }

        .image-preview {
            margin-top: 15px;
            width: 100%;
            height: 200px;
            object-fit: cover;
            border-radius: 12px;
            border: 1px solid #ddd;
        }

        button {
            padding: 12px;
            width: 100%;
            background: #f4b400;
            color: white;
            border: none;
            border-radius: 10px;
            font-size: 15px;
            font-weight: 600;
            cursor: pointer;
            margin-top: 15px;
        }

        button:hover {
            background: #d99a00;
        }

        .back {
            text-decoration: none;
            display: block;
            margin-bottom: 20px;
            color: #444;
            font-size: 14px;
        }
    </style>
</head>

<body>

    <div class="navbar">
        <div class="logo">HomeBitez</div>
    </div>

    <div class="page-title">Edit Product</div>

    <div class="container">

        <a class="back" href="/owner/inventory">← Back to Inventory</a>

        <form action="/owner/edit/<%= product.id %>" method="POST" enctype="multipart/form-data">

            <label>Product Name</label>
            <input type="text" name="productName" value="<%= product.productName %>" required>

            <label>Quantity</label>
            <input type="number" name="quantity" value="<%= product.quantity %>" required>

            <label>Price (S$)</label>
            <input type="number" step="0.01" name="price" value="<%= product.price %>" required>

            <label>Current Image</label>
            <img src="/images/<%= product.image %>" class="image-preview">

            <label>Upload New Image</label>
            <input type="file" name="imageFile" accept="image/*">

            <!-- Hidden field to keep old image if user does not upload a new one -->
            <input type="hidden" name="currentImage" value="<%= product.image %>">

            <button type="submit">Save Changes</button>
        </form>

    </div>

</body>

</html>
