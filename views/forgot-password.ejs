<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Forgot Password - HomeBitez</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Sniglet:wght@700;900&display=swap" rel="stylesheet">
  <style>
    body { background-color:#f8f3ed; font-family: Arial, sans-serif; }
    .container-box { display:flex; height:100vh; }
    .left-box { width:50%; background:white; padding:80px; display:flex; flex-direction:column; justify-content:center; }
    .right-box { width:50%; background-color:#ffe9d6; display:flex; justify-content:center; align-items:center; padding:40px; }
    .right-box img { width:80%; max-width:500px; height:100%; max-height:600px; border-radius:10%; object-fit:cover; box-shadow:0 8px 20px rgba(0,0,0,0.15); }
    .logo { font-family:'Sniglet', cursive; font-size:40px; font-weight:900; margin-bottom:30px; line-height:1; text-shadow:2px 2px 2px rgba(0,0,0,0.1); }
    .logo span.home { color:#f4b400; }
    .logo span.bitez { color:#FF8C1A; }
    .form-control { background:#f4f4f4; border-radius:25px; height:45px; border:none; }
    .login-btn { width:100%; height:45px; background-color:black; color:white; border-radius:25px; border:none; font-size:16px; margin-top:10px; }
    a { text-decoration:none; color:#666; }
    .msg { font-size:14px; margin-bottom:12px; }
  </style>
</head>
<body>
  <div class="container-box">
    <div class="left-box">
      <div class="logo">
        <span class="home">Home</span><span class="bitez">Bitez</span>
      </div>
      <h4>Forgot your password?</h4>
      <p class="msg">Enter your email and we’ll send a reset link.</p>

      <% if (success && success.length > 0) { %>
        <div class="alert alert-success"><%= success %></div>
      <% } %>
      <% if (error && error.length > 0) { %>
        <div class="alert alert-danger"><%= error %></div>
      <% } %>

      <form action="/forgot-password" method="POST">
        <input type="email" name="email" placeholder="Your Email" class="form-control mb-3" required />
        <button class="login-btn" type="submit">Send Reset Link</button>
      </form>

      <div class="text-center mt-3">
        <a href="/login">Back to login</a>
      </div>
    </div>
    <div class="right-box">
      <img src="/images/flatlay-iron-skillet-with-meat-and-other-food.jpg" alt="Food">
    </div>
  </div>
</body>
</html>
