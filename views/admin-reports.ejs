<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>HomeBitez - Sales Report</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Sniglet:wght@700;900&family=Sora:wght@300;400;600;700&display=swap" rel="stylesheet">
  <style>
    :root {
      --cream: #fff5ea;
      --cream-deep: #fde8cf;
      --card: #ffffff;
      --ink: #1d1d1d;
      --muted: #6c6c6c;
      --accent: #f0a400;
      --accent-deep: #e38f00;
      --shadow: 0 12px 28px rgba(20, 20, 20, 0.08);
      --line: rgba(20, 20, 20, 0.12);
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: "Sora", sans-serif;
      color: var(--ink);
      background:
        radial-gradient(1200px 600px at 10% -10%, #fff1de 0%, transparent 60%),
        linear-gradient(180deg, var(--cream) 0%, #fff8f0 100%);
      min-height: 100vh;
    }

    .topbar {
      position: sticky;
      top: 0;
      z-index: 5;
      background: var(--cream-deep);
      box-shadow: 0 4px 12px rgba(20, 20, 20, 0.08);
      padding: 18px 40px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 16px;
    }

    .brand {
      font-family: "Sniglet", cursive;
      font-size: 30px;
      font-weight: 900;
      color: var(--accent);
      text-decoration: none;
      letter-spacing: 0.5px;
    }

    .nav-links a {
      text-decoration: none;
      color: var(--ink);
      margin: 0 14px;
      font-size: 14px;
      font-weight: 600;
    }

    .welcome {
      font-weight: 600;
      font-size: 15px;
    }

    .page {
      max-width: 1100px;
      margin: 40px auto 96px;
      padding: 0 28px;
    }

    .card {
      background: var(--card);
      border-radius: 18px;
      box-shadow: var(--shadow);
      border: 1px solid var(--line);
      padding: 28px;
    }

    .title {
      text-align: center;
      font-size: 24px;
      font-weight: 700;
      margin-bottom: 18px;
    }

    .stats {
      display: grid;
      grid-template-columns: repeat(5, minmax(140px, 1fr));
      gap: 14px;
      margin-bottom: 20px;
    }

    .stat {
      background: #f6f6f6;
      border-radius: 12px;
      padding: 12px;
      text-align: center;
      font-size: 12px;
      font-weight: 600;
      min-height: 86px;
      display: flex;
      flex-direction: column;
      justify-content: center;
    }

    .stat strong {
      display: block;
      font-size: 16px;
      margin-top: 6px;
    }

    .grid {
      display: grid;
      grid-template-columns: 2fr 1fr;
      gap: 18px;
    }

    .panel {
      background: #f6f6f6;
      border-radius: 14px;
      padding: 16px;
    }

    .panel-title {
      font-size: 13px;
      font-weight: 700;
      margin-bottom: 10px;
      text-align: center;
    }

    .chart {
      height: 220px;
      display: flex;
      align-items: flex-end;
      gap: 10px;
      padding: 14px 10px 6px;
      border-radius: 12px;
      background: #fff;
    }

    .bar {
      flex: 1;
      background: linear-gradient(180deg, #38a3ff 0%, #0f5bbd 100%);
      border-radius: 8px 8px 4px 4px;
      position: relative;
      min-height: 6px;
    }

    .bar::after {
      content: attr(data-label);
      position: absolute;
      bottom: -20px;
      left: 50%;
      transform: translateX(-50%);
      font-size: 10px;
      color: var(--muted);
    }


    .side-panels {
      display: grid;
      gap: 14px;
    }

    .best-item {
      display: flex;
      align-items: center;
      gap: 12px;
    }

    .food {
      width: 54px;
      height: 54px;
      border-radius: 10px;
      background: #f0a400;
      color: #fff;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      font-size: 12px;
      font-weight: 700;
    }

    .metric {
      font-size: 22px;
      font-weight: 700;
      text-align: center;
    }

    .muted {
      color: var(--muted);
      font-size: 12px;
      font-weight: 600;
    }

    .refresh {
      display: flex;
      align-items: center;
      justify-content: center;
      height: 90px;
      font-size: 42px;
    }

    .refresh-btn {
      width: 100%;
      border: none;
      border-radius: 12px;
      height: 72px;
      background: #fff;
      font-size: 26px;
      font-weight: 700;
      cursor: pointer;
      box-shadow: inset 0 0 0 1px rgba(20, 20, 20, 0.08);
    }

    @media (max-width: 980px) {
      .stats {
        grid-template-columns: repeat(2, minmax(140px, 1fr));
      }
      .grid {
        grid-template-columns: 1fr;
      }
    }

    @media (max-width: 600px) {
      .nav-links {
        display: flex;
        flex-wrap: wrap;
        gap: 8px;
      }
      .nav-links a {
        margin: 0;
      }
      .page {
        padding: 0 18px;
      }
    }
  </style>
</head>
<body>
  <header class="topbar">
    <a class="brand" href="/">HomeBitez</a>
    <nav class="nav-links">
      <a href="/admin">Home</a>
      <a href="/">Menu</a>
      <a href="/admin/inventory">Inventory</a>
    </nav>
    <div class="welcome">Welcome, <%= adminName %>!</div>
  </header>

  <main class="page">
    <section class="card">
      <h1 class="title">Performance Dashboard</h1>
      <div class="stats">
        <div class="stat">Date Range<strong><%= monthLabel %></strong></div>
        <div class="stat">Total Revenue<strong>$<%= totalRevenue.toLocaleString("en-US", { minimumFractionDigits: 2, maximumFractionDigits: 2 }) %></strong></div>
        <div class="stat">Total Cost<strong>$<%= totalCost.toLocaleString("en-US", { minimumFractionDigits: 2, maximumFractionDigits: 2 }) %></strong></div>
        <div class="stat">Total Profit<strong>$<%= totalProfit.toLocaleString("en-US", { minimumFractionDigits: 2, maximumFractionDigits: 2 }) %></strong></div>
        <div class="stat">Total Orders<strong><%= totalOrders %></strong></div>
      </div>

      <div class="grid">
        <div class="panel" style="position: relative;">
          <div class="panel-title">Performance Report</div>
          <div class="chart">
            <% const maxValue = Math.max(...chartValues, 1); %>
            <% chartValues.forEach((value, idx) => { %>
              <% const height = Math.max((value / maxValue) * 100, 6); %>
              <div class="bar" style="height: <%= height %>%;" data-label="<%= chartLabels[idx] %>"></div>
            <% }) %>
          </div>
        </div>

        <div class="side-panels">
          <div class="panel">
            <div class="panel-title">Best Performing Food</div>
            <div class="best-item">
              <div class="food">HB</div>
              <div>
                <div><%= bestItem.name %></div>
                <div class="muted">$<%= Number(bestItem.revenue || 0).toLocaleString("en-US", { minimumFractionDigits: 2, maximumFractionDigits: 2 }) %></div>
              </div>
            </div>
          </div>

          <div class="panel">
            <div class="panel-title">Growth vs Last Month</div>
            <div class="metric"><%= growthPercent.toFixed(1) %>%</div>
          </div>

          <div class="panel">
            <div class="panel-title">Average Order Value</div>
            <div class="metric">$<%= averageOrderValue.toFixed(2) %></div>
          </div>

          <div class="panel">
            <div class="panel-title">Refresh Data?</div>
            <button class="refresh-btn" type="button" id="refreshReport">R</button>
          </div>
        </div>
      </div>
    </section>
  </main>

  <script>
    const refreshButton = document.getElementById("refreshReport");
    if (refreshButton) {
      refreshButton.addEventListener("click", () => {
        window.location.reload();
      });
    }
  </script>
</body>
</html>
